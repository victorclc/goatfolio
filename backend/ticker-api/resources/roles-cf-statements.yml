- Effect: Allow
  Action:
    - dynamodb:Query
    - dynamodb:GetItem
    - dynamodb:BatchGetItem
  Resource:
    - 'Fn::ImportValue': ${self:provider.stage}-TickerInfoTableArnOutput
    - Fn::Join:
        - ''
        - - 'Fn::ImportValue': ${self:provider.stage}-TickerInfoTableArnOutput
          - '/index/*'
