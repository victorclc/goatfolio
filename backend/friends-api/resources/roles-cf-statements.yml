- Effect: Allow
  Action:
    - dynamodb:Query
    - dynamodb:GetItem
    - dynamodb:PutItem
    - dynamodb:BatchWriteItem
  Resource:
    - !GetAtt FriendsTable.Arn
    - Fn::Join:
        - ''
        - - !GetAtt FriendsTable.Arn
          - '/index/*'
- Effect: Allow
  Action:
    - cognito-idp:ListUsers
  Resource:
    - 'Fn::ImportValue': ${self:provider.stage}-GoatfolioAppUserPoolArnOutput
    - 'Fn::ImportValue': ${self:provider.stage}-NewGoatfolioAppUserPoolArnOutput
